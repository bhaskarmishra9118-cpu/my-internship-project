version: '3.8'

services:
  backend:
    build: ./Bakend
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build: ./Frontend/vite-project
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
giyt